---
subtitle: "Analyse simple du texte"
cache: false
---
::: {.panel-tabset}

# Généralités

On ne travaille que sur les mots (ou pour être exact les racines des mots) en analysant uniquement leur fréquence dans le texte.


## Méthodologie statistique

L'étude comprend deux partie : 

- un questionnaire simple dont toutes les réponses sont qualitatives ou ont pu être discrétisées. 
-  Des entretiens dirigés en texte brut. 

### Questionnaires

Vu le très faible nombre de cas aucun test ne eut être réalisé, l'analyse sera uniquement descriptive. Plusieurs tableaux présentant les réponses seront réalisés (nombre & pourcentage)
 :

- Profil du répondant
- Initiation d’une thérapie orale
- Analyse médicamenteuse et conciliation
- Évaluation globale du patient
- Recours aux soins de support
- Suivi des patients sous thérapies orales
- Organisation future

### Entretiens

L'analyse des entretiens a été réalisée en *text mining*. L'analyse a été uniquement descriptive & concentrée sur les mots les plus fréquents et les associations de mots.

Une première étape a été la transformation du texte en extrayant les mots qui sont réduits à leur racine (suppression des conjugaisons, des accords etc. Ensuite les mots considérés comme *vides* (pronoms, auxiliaires, verbe courants...) ont été supprimés pour ne garder que les mots considérés comme intéressants. Pour chaque répondant les mots les plus utilisés ont été présentés en tableau puis sur deux graphiques : graphique en barre & nuage de mots (package. Une comparaison des réponses entre les participants a été présentée. Enfin les associations de pot avev les mots les plus fréquents a été réalisée par une analyse en corrélation. Le seuil retenue pour suggérer une corrélation a été un R supérieur à 0,6.

Vu le très faible nombre de cas l'analyse du questionnaire est uniquement descriptive. Les résultats sont présentés sous forme de tableaux (nombre et pourcentage) et de graphiques. 

L'analyse statistique a été réalisée avec le logiciel **R**[@rstat] & divers packages. 

```{r}
#| label: setup
rm(list = ls())

library("tidyverse")
library("dplyr")
library("readODS")
library("gtsummary")
library("kableExtra")
library("plotly")
library("tm")
library("proustr")
library("ggsci")
library("tidytext")
library("SnowballC")
library("ggwordcloud")
library("RColorBrewer")
library("baseph")
library("ggstats")

expx <- FALSE
classeur <- "hdjto.ods"
#
theme_gtsummary_language(language = "fr", decimal.mark = ",")
# theme_gtsummary_journal(journal = "jama")
options(OutDec = ",")
```

```{r}
#| label: import

tt <- NULL
htdoc <- NULL
nn <- 0
prof <- c("IDE en consultation", "Assistante sociale", 
          "Cadre supérieure de santé", "Pharmacien 1",
          "Pharmacien 2", "Cadre faisant fonction",
          "Hématologue", "Secrétaire médicale")

mesmots <- as_tibble(c("c'est", "ça", "donc", "fait", "parce",
                       "peut","quand","c'et","ca","qu’il", "être", "puis", "aussi", "plus", "fois", "là", "sous",
             "après", "peux", "alors", "beaucoup", "comme", "quil",
             "faut", "voilà", "j'ai", "n’est", "vont","oui", "peutêtr",
             "ben", "peu", "tout", "car", "quelqu","qu'on",
             "qu'il","c’est", "qu’on","j’ai", "qu'el", "peuvent", "ell",
             "aller", "donné", "ver", "doit", "donc", "dirai",
             "puisqu","ell","vraiment", "évidem", "non","jai", "sai","déjà", "qu'il", "n'y", "qu'on", "n’ai","n’y", "tous", "avoir","estc", "autr", "...", "quon", "ouai", "entr", "dire", "dirai" ,"commen", "voi"))


for(nt in 1:8){
nomtxt <- paste0("datas/entretien",nt,".txt")
texte <- readr::read_file(nomtxt)
profession <- prof[nt]
ll <- c(nt,profession,texte)
tt <- rbind(tt,ll)
}
stopw <- proust_stopwords()
stopw <- stopw$word

tt <- as_tibble(tt)
names(tt) <- c("id", "profession", "texte")

# Racines, mots significatifs, suppression des mots vide

zz <- tt |> 
tidytext::unnest_tokens(output="mots",
                        input=texte,
                        token="words") |> 
  anti_join(proustr::proust_stopwords(),by=c("mots"="word")) |> 
  anti_join(as_tibble(mesmots),by=c("mots"="value")) |> 
  dplyr::filter(!mots %in% mesmots$value) |> 
  dplyr::filter(!mots %in% get_stopwords("fr")$word) |> 
  mutate(mots = wordStem(mots, language = "fr"))
```




```{r}
#| label: macro-tabfq
tabfq <- function(dd){
kbl(head(dd, 10)) |>
  kable_material(c("striped", "hover"))
} |> 
   kable_styling(fixed_thead = TRUE) |> 
    scroll_box(width = "100%", height = "80%")
```

```{r}
#| label: macro-nuage

nuage <- function(d = docs){
zz |> 
  count(mots, sort = TRUE) |> 
  head(20) |> 
  ggplot() +
    theme_grey() +
  aes(label = mots, size= n, color = n) +
  geom_text_wordcloud(grid_size = 20) +
   scale_size_area(max_size = 20) +
 scale_color_viridis_c(option = "turbo", direction = 1) +
  theme(
    legend.position = "none",
    plot.title = element_text(size = 14, face = "bold"),
    plot.subtitle = element_text(size = 12),
    axis.title.y.right = element_blank(),
    legend.title = element_text(size = 12),
    axis.title.y = element_blank(),
    axis.text.x = element_blank(),
    axis.text.y = element_blank()
  )
} 
```

```{r}
#| label: macro-figfq

figfq <- function(d = docs,nmax = 6){
  gg <- d |> 
  dplyr::filter(n > nmax) |> 
  ggplot() +
  aes(x = fct_reorder(mots,n, .desc = FALSE), y = n, fill = n) +
  geom_bar(stat="identity") +
  labs(
    title = "Mots les plus fréquents - IDE en consultation",
    y = "Fréquence",
    caption = "Nombre d'occurence des mots significatifs (réduits à leur racine)  les plus fréquents.",
  ) +
  theme_light() +
   scale_color_viridis_c(option = "turbo", direction = 1) +
  theme(
    plot.title = element_text(size = 14, face = "bold"),
    plot.subtitle = element_text(size = 12),
    axis.title.y.right = element_blank(),
    legend.title = element_text(size = 12),
    axis.title.y = element_blank(),
    axis.text.x = element_text(size = 12),
    axis.text.y = element_text(size = 12),
    legend.position = "none") +
    coord_flip() 
  
ggplotly(gg)
}
```


### Bibiliographie

::: {#refs}
:::

# Entretiens

::: {.panel-tabset group="Entretiens"}

## IDE en consultation

```{r}
#| label: intro

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)
```




```{r}
#| label: tbl-MOTS1
#| tbl-cap: "Mots les plus fréquents - IDE en consultation"

tabfq(docs)
```

```{r}
#| label: fig-nuage1
#| fig-cap: "Mots les plus fréquents - IDE en consultation"
#| fig-alt: "Mots les plus fréquents - IDE en consultation. Les valeurs sont dans le tableau précédent"

nuage(docs)
```

```{r}
#| label: fig-MOTS1
#| fig-cap: "Mots les plus fréquents - IDE en consultation"
#| fig-alt: "Mots les plus fréquents - IDE en consultation. Les valeurs sont dans le tableau précédent"

figfq()
```

## Assistante sociale


```{r}
#| label: tbl-mots2
#| tbl-cap: "Mots les plus fréquents - Assistante sociale"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)
```

```{r}
#| label: fig-nuage2
#| fig-cap: "Mots les plus fréquents - Assistante sociale"
#| fig-alt: "Mots les plus fréquents - Assistante sociale. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-mots2
#| fig-cap: "Mots les plus fréquents - Assistante sociale"
#| fig-alt: "Mots les plus fréquents - Assistante sociale. Les valeurs sont dans le tableau précédent"

figfq(nmax = 7)
```

## Cadre supérieure de santé


```{r}
#| label: tbl-mots3
#| tbl-cap: "Mots les plus fréquents - Cadre supérieure de santé"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)

```

```{r}
#| label: fig-nuage3
#| fig-cap: "Mots les plus fréquents - Cadre supérieure de santé"
#| fig-alt: "Mots les plus fréquents - Cadre supérieure de santé. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-MOTS3
#| fig-cap: "Mots les plus fréquents - Cadre supérieure de santé"
#| fig-alt: "Mots les plus fréquents - Cadre supérieure de santé. Les valeurs sont dans le tableau précédent"

figfq(nmax = 7)
```

## Pharmacien 1


```{r}
#| label: tbl-mots4
#| tbl-cap: "Mots les plus fréquents - Pharmacien"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)

```

```{r}
#| label: fig-nuage4
#| fig-cap: "Mots les plus fréquents - Pharmacien"
#| fig-alt: "Mots les plus fréquents - Pharmacien. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-mots4
#| fig-cap: "Mots les plus fréquents - Pharmacien"
#| fig-alt: "Mots les plus fréquents - Pharmacien. Les valeurs sont dans le tableau précédent"

figfq(nmax = 7)
```

## Pharmacien 2

**`r prof[5]`**

```{r}
#| label: tbl-mots5
#| tbl-cap: "Mots les plus fréquents - Pharmacien"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)
```

```{r}
#| label: fig-nuage5
#| fig-cap: "Mots les plus fréquents - Pharmacien"
#| fig-alt: "Mots les plus fréquents - Pharmacien. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-mots5
#| fig-cap: "Mots les plus fréquents - Pharmacien"
#| fig-alt: "Mots les plus fréquents - Pharmacien. Les valeurs sont dans le tableau précédent"

figfq(nmax = )
```

## Cadre faisant fonction

**`r prof[6]`**

```{r}
#| label: tbl-mots6
#| tbl-cap: "Mots les plus fréquents - Cadre faisant fonction"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)
```

```{r}
#| label: fig-nuage6
#| fig-cap: "Mots les plus fréquents - Cadre faisant fonction"
#| fig-alt: "Mots les plus fréquents - Cadre faisant fonction. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-mots6
#| fig-cap: "Mots les plus fréquents - Cadre faisant fonction"
#| fig-alt: "Mots les plus fréquents - Cadre faisant fonction. Les valeurs sont dans le tableau précédent"

figfq(nmax = 7)
```

## Hématologue

**`r prof[7]`**

```{r}
#| label: tbl-mots7
#| tbl-cap: "Mots les plus fréquents - Hématologue"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)
```

```{r}
#| label: fig-nuage7
#| fig-cap: "Mots les plus fréquents - Hématologue"
#| fig-alt: "Mots les plus fréquents - Hématologue. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-mots7
#| fig-cap: "Mots les plus fréquents - Hématologue"
#| fig-alt: "Mots les plus fréquents - Hématologue. Les valeurs sont dans le tableau précédent"

figfq(nmax = 3)
```

## Secrétaire médicale

**`r prof[8]`**

```{r}
#| label: tbl-mots8
#| tbl-cap: "Mots les plus fréquents - Secrétaire médicale"

nn <- nn +1
nprof = prof[nn]
docs <- zz |> 
  dplyr::filter(profession == nprof) |> 
  dplyr::count(mots, sort = TRUE)
```

```{r}
#| label: fig-nuage8
#| fig-cap: "Mots les plus fréquents - Secrétaire médicale"
#| fig-alt: "Mots les plus fréquents - Secrétaire médicale. Les valeurs sont dans le tableau précédent"

nuage()
```

```{r}
#| label: fig-mots8
#| fig-cap: "Mots les plus fréquents - Secrétaire médicale"
#| fig-alt: "Mots les plus fréquents - Secrétaire médicale. Les valeurs sont dans le tableau précédent"

figfq(nmax = 2)
```

:::

# Comparaisons

```{r}
# Comparaisons
#| label: tbl-mots-comp
#| tbl-cap: "Mots les plus fréquents"

docs <- zz |> 
  dplyr::count(mots, sort = TRUE) |> 
  head(10) 

czz <- zz |> 
  dplyr::filter(mots %in% docs$mots) |> 
  dplyr::count(profession, mots, sort = TRUE)|> 
  mutate(across(is.character, as.factor))
```

```{r}
#| label: comp-prof
#| fig-cap: Mots les plus fréquents par profession
#| fig-asp: 1.8 

aa <- czz |> 
  ggplot() +
  aes(x = fct_reorder(mots,n, .desc = TRUE), y = n, fill = profession) +
  geom_bar(stat="identity", position = "dodge") +
  labs(
    title = "Mots les plus fréquents par profession",
    caption = "Nombre d'occurences des mots significatifs
    (réduits à leur racine) les plus fréquents."
  ) +
  theme_light() +
scale_color_viridis_c() +
  theme(
    plot.title = element_text(size = 14, face = "bold"),
    plot.subtitle = element_text(size = 12),
    axis.title.y.right = element_blank(),
    legend.title = element_text(size = 12),
    axis.title.y = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_text(size = 12),
    axis.text.y = element_blank(),
    legend.position = "none") +
    facet_grid(profession ~.,
               scales = "free") 
ggplotly(aa)
```

```{r}
#| label: comp-mots
#| fig-cap: Mots les plus fréquents 
#| fig-asp: 1.8 

aa <- czz |> 
  ggplot() +
  aes(x = profession, y = n, fill = mots) +
  geom_bar(stat="identity", position = "dodge") +
  labs(
    title = "Mots les plus fréquents par profession",
    caption = "Nombre d'occurences des mots significatifs
    (réduits à leur racine) les plus fréquents."
  ) +
  theme_light() +
scale_color_viridis_c() +
  theme(
    plot.title = element_text(size = 14, face = "bold"),
    plot.subtitle = element_text(size = 12),
    axis.title.y.right = element_blank(),
    legend.title = element_text(size = 12),
    axis.title.y = element_blank(),
    axis.title.x = element_blank(),
    axis.text.x = element_text(size = 10, angle = 50, hjust = 1),
    axis.text.y = element_blank(),
    legend.position = "none") +
    facet_grid(mots ~.,
               scales = "free")

ggplotly(aa)
```

# Association de mots

Le mot qui revient le plus souvent est "*patient*". On peut donc s'intéresser
aux mots qui sont associés à ce mot. On va donc créer un tableau de contingence
entre le mot "*patient*" et les autres mots significatifs. On prend comme
limite un coefficient de corrélation supérieur à 0,6.
s
```{r}
#| label: tbl-assoc-patient
#| tbl-cap: Mots associés au mot "patient" (corrélation > 0.6)

docs <- Corpus(VectorSource(tt$texte))
docs <- tm_map(docs, content_transformer(tolower))
docs <- tm_map(docs, removeNumbers)
docs <- tm_map(docs, removeWords, stopwords("french"))
docs <- tm_map(docs, removeWords, mesmots$value) 
docs <- tm_map(docs, removeWords, "ver") 
docs <- tm_map(docs, removePunctuation)
docs <- tm_map(docs, stripWhitespace)
docs <- tm_map(docs, stemDocument)
dtm <- TermDocumentMatrix(docs)
aap <- findAssocs(dtm, terms = "patient", corlimit = 0.59)

aa <- as_tibble(aap$patient, rownames = "mots") |>
  dplyr::filter(! mots == "ver") |> 
  dplyr::rename(corr = value) |>
  dplyr::arrange(desc(corr)) |> 
  ggplot() +
  aes( x =  fct_reorder(mots, corr, .desc = TRUE), y = corr, fill = corr) +
    geom_col() +
  scale_fill_viridis_c() +
    theme_light() +
    labs(
    title = "Mots associés au mot \"patient\"",
    caption = "Mots associés au mot \"patient\" (cœfficient de corrélation
    supérieur à 0.6)", 
    y = "Coefficient de corrélation"
  ) +
  theme(
    plot.title = element_text(size = 14, face = "bold"),
    plot.subtitle = element_text(size = 12),
    axis.title.y.right = element_blank(),
    legend.title = element_text(size = 12),
    axis.title.y = element_text(size = 12),
    axis.title.x = element_blank(),
    axis.text.x = element_text(size = 10, angle = 50, hjust = 1),
    axis.text.y = element_text(size = 10),
    legend.position = "none")

ggplotly(aa)
```



# Questionnaire

::: {.panel-tabset group="qQuestionnaires"}

```{r}
#| label: Import


tt <- readODS::read_ods("datas/HDJ-TO.ods") |> 
  mutate(across(is.character, as.factor)) |> 
  mutate(across(Comprehension_verifiee:Comprehension_verifiee, ~fct_relevel(.x,
    "Toujours", "Parfois", "Rarement", "Jamais"
  ))) |> 
    mutate(Evaluation_fragilite = fct_relevel(Evaluation_fragilite,
    "Toujours", "Parfois"
  )) |> 
  mutate(across(starts_with("Recours"),~ fct_relevel(.x,                                                     "Systématique",
  "Selon situation",
  "Rarement", "Jamais"))) |> 
  mutate(across(starts_with("Ancien"),~ fct_relevel(.x, "0-5ans", "6-10ans",
                                                    ">10ans"))) |> 
  mutate(Temps_information = fct_relevel(Temps_information, 
                                         "10-20min", ">20min")) |> 
  mutate(Acces_soins_support_suffisant = fct_relevel(Acces_soins_support_suffisant, 
                                         "Oui","Partiellement" ,"Non"))
```


## Profil du répondant

```{r}
#| label: tbl-profil
#| tbl-cap: Profil du répondant

tt |> 
  dplyr::select(Fonction:Initie_TO) |> 
  tbl_summary() |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "profil", lg = TRUE) 
```

## Initiation d’une thérapie orale

```{r}
#| label: tbl-initto
#| tbl-cap: "Initiation d’une thérapie orale"

tt |> 
  dplyr::select(Initiation_mode:Comprehension_verifiee) |> 
  tbl_summary() |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "initto", lg = TRUE) 
```

## Analyse médicamenteuse et conciliation

```{r}
#| label: tbl-concil
#| tbl-cap: "Analyse médicamenteuse et conciliation"

tt |> 
  dplyr::select(Ord_MT_analyse:Interactions_identifiees) |> 
  tbl_summary() |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "concil", lg = TRUE) 
```

## Évaluation globale du patient


```{r}
#| label: tbl-eval
#| tbl-cap: "Évaluation globale du patient"

tt |> 
  dplyr::select(starts_with("Evalu")) |> 
  pivot_longer(cols = everything(), names_to = "Evaluation", 
               values_to = "Réponse") |>
  tbl_summary(by = "Réponse") |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "eval", lg = TRUE) 
```


```{r}
#| label: fig-eval
#| fig-cap: "Évaluation globale du patient"

aa <- tt |> 
  dplyr::select(starts_with("Evalu")) |> 
  gglikert() +
   scale_color_viridis_c(option = "turbo", direction = 1) + 
  labs(title = "Évaluation globale du patient")

ggplotly(aa)
```

## Recours aux soins de support


```{r}
#| label: tbl-recours
#| tbl-cap: "Recours aux soins de support"

tt |> 
  dplyr::select(starts_with("Recours")) |> 
  pivot_longer(cols = everything(), names_to = "Evaluation", 
               values_to = "Réponse") |> 
    mutate(Réponse = fct_relevel(Réponse,                                               "Systématique",
  "Selon situation",
  "Rarement", "Jamais")) |> 
  tbl_summary(by = "Réponse") |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "recours", lg = TRUE) 
```


```{r}
#| label: fig-recours
#| fig-cap: "Recours aux soins de support"

aa <- tt |> 
  dplyr::select(Recours_ETP,Recours_dietetique,Recours_psychologue
                ,Recours_assistante_sociale) |> 
  gglikert(sort = "descending") +
   scale_color_viridis_c(option = "turbo", direction = 1) + 
  labs(title = "Recours aux soins de support")

ggplotly(aa)
```

```{r}
#| label: tbl-recours2
#| tbl-cap: "Recours aux soins de support"

tt |> 
  dplyr::select(Acces_soins_support_suffisant) |> 
  tbl_summary() |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "recours2", lg = TRUE) 
```


## Suivi des patients sous thérapies orales


```{r}
#| label: tbl-suivi
#| tbl-cap: "Suivi des patients sous thérapies orales"

tt |> 
  dplyr::select(c(Analyse_biologie, EI_detectes_tardivement:Denutrition_non_reperee)) |> 
  tbl_summary() |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "suivi", lg = TRUE) 
```

```{r}
#| label: suivi2
#| tbl-cap: = "Modalités du suivi"

zz <- str_split(tt$Modalites_suivi, pattern = "/") |> 
  unlist() |> 
  as_tibble() |> 
  group_by(value) |> 
  summarise(n = n()) |> 
  rename('Modalités de suivi' = value) |>
  arrange(desc(n)) 

zz |> 
  kbl(escape = TRUE, longtable = lg, booktabs = TRUE) |>
    kable_styling(latex_options = c("scale_down", "repeat_header"))

if(expx) {
 write_ods(zz,path = classeur, sheet = "modsuivi", append = TRUE)
}
```

```{r}
#| label: suivi3
#| tbl-cap: = "Gestion_anomalie"

zz <- str_split(tt$Gestion_anomalie, pattern = "/") |> 
  unlist() |> 
  as_tibble() |> 
  group_by(value) |> 
  summarise(n = n()) |> 
  rename('Gestion_anomalie' = value) |>
  arrange(desc(n)) 

zz |> 
  kbl(escape = TRUE, longtable = lg, booktabs = TRUE) |>
      kable_styling(latex_options = c("scale_down", "repeat_header"))


if(expx) {
 write_ods(zz, path = classeur, sheet = "gestano", append = TRUE)
}
```

## Organisation future


```{r}
#| label: tbl-futur
#| tbl-cap: "Organisation future"

tt |> 
  dplyr::select(Parcours_structurer_utile:Experience_patient) |> 
  tbl_summary() |> 
     bold_labels() |> 
   modify_header(label ~ " ") |>
  pexptabph(exp = expx, nomfich = classeur , nomsheet = "futur", lg = TRUE) 
```

## Priorités

```{r}

#| label: priorite
#| tbl-cap: Priorités

zz <- tt |> 
  dplyr::select(starts_with("Priorite")) |>
  summarise(across(everything(), ~ sum(.x))) |> 
  pivot_longer(cols = everything(), names_to = "Priorité", values_to = "Note") |> 
   arrange(desc(Note))

zz |> 
    kbl(escape = TRUE, longtable = lg, booktabs = TRUE) |>
      kable_styling(latex_options = c("scale_down", "repeat_header"))


if(expx) {
 write_ods(zz, path = classeur, sheet = "priorites", append = TRUE)
}
```

:::

:::
